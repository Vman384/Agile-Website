import { BrowserRouter as Router, Route, Routes } from "react-router-dom"
import {}
export const Navigation = () =>{
    return(
        <Router>
            <Routes>
                <Route path = "/Login">
                    <
                </Route>
            </Routes>
        </Router>
    )

}